[pytest]
# 默认只跑非 slow、非 integration 的快速测试
# 跑全部：pytest -m ""
# 跑 slow：pytest -m slow
addopts = -m "not slow and not integration" --strict-markers
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning
