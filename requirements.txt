# requirements.txt — 志鉴项目精简版（OCR + KG + RAG）
# 默认含 RapidOCR（ONNX 后端，约 50MB 模型首次运行自动下载）。
# 阿里云 OCR 仅需环境变量，无需额外依赖。

# 核心框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# OCR 引擎（默认）
rapidocr-onnxruntime==1.3.5

# 图像处理（前端文件上传、OCR 预处理、RAG 图片嵌入兜底）
opencv-python==4.9.0.80
Pillow==10.2.0

# 深度学习（KG 抽取、BGE Embedder 也走 torch）
torch==2.1.2
transformers==4.37.2

# 向量数据库
chromadb==1.5.5
sentence-transformers==5.3.0

# 数值与科学计算
numpy==1.26.3
scipy==1.12.0

# 工具库
PyYAML==6.0.1
tqdm==4.66.1
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
python-multipart==0.0.6

# ==================== OCR 引擎（可选） ====================
# 以下引擎默认不装，按需手动 pip install。
#
# EasyOCR —— 备选引擎，跨平台，CPU/GPU 双模
# 安装：pip install easyocr==1.7.1
# 注意：首次运行会下载 ~100MB 模型到 ~/.EasyOCR/model/
#
# PaddleOCR —— Linux/Mac 备选（Windows 3.13 兼容问题多）
# 安装：pip install paddlepaddle==2.6.1 paddleocr==2.7.3
#
# AliyunOCR —— 云端 API（古籍识别质量最高，消耗云端配额）
# 依赖：仅 stdlib urllib（已含）
# 配置：export ALIYUN_OCR_APP_CODE=xxx
# 或：export ALIYUN_ACCESS_KEY_ID=xxx ALIYUN_ACCESS_KEY_SECRET=xxx
